{
  "name": "thegodofme",
  "version": "1.0.0",
  "description": "The God of Me — one-page marketing site",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "npx --yes serve src -l 3000",
    "preview": "npx --yes serve dist -l 3000"
  }
}
